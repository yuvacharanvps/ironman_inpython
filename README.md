# IRON MAN FACE USING PYTHON!

# ABOUT THIS FILE:
-This python file uses the in-built module turtle to construct iron man image.

## Project Structure
-The code was typed in gitbash command line.

-->ITS STRUCTURE INCLUDES :
- created a folder name ironman_python using the "mkdir" command.
- using the "cd" command I entered the folder created.
- later using "vi" command I created the `ironman.py` file.
- then using "git init" I intilized the folder.
- then I also created a repo in git hub with the same name and copied its link.
- then I used "git remote add origin 'link' " to connect my repo with the remote repo.
- then I added some text in the `ironman.py` file pushed it to my remote repo.
- then I create two branches 'sub1' and 'sub2'.
- then I used "git checkout sub1" to switch to sub1 and added few lines of code,then used "git add ." and "git commit -m "" ", and finally pushed the branch to remote branch using "git push origin sub1" command.
- then I did the same thing i did in "sub1" in "sub2".
- now coming to the main part, that is combaining the whole code (# mergin).
- first i merged sub2 in sub1 using "git merge sub2" command there was a conflit in auto merging therfore did manual mergining using "git mergetool" command, the merging was successful.
- then later i switched to "master" branch and merged "sub1" in "master" branch.
- then later after checking the whole code I finnaly pushed the "master" branch to my remote repo.

## Known Limitations
- checked in only vs code, dont know if the code works in other compiler.

## Contributing
-Pull requests are welcome. For major changes, open an issue first to discuss what you'd like to change.

## NOTE FOR FUTURE
- after creating the README file, go to local repo and pull the remote repo to update it.
